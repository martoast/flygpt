# User-requested MacBook-to-M4 migration — 2026-09-15

The user needs the MacBook for another project and requested moving its remaining work to the Mac mini. At migration, seed 505 E_relational, D_hidden_shuffled and F_relational_shuffled were complete on M1; C_hidden was stopped with a verified checkpoint at update 96. Resume C_hidden from that optimizer/RNG/model state on M4; run the remaining seed-505 methods and all seeds 506–509 on M4. Keep every original condition, budget, example schedule and test gate unchanged. Preserve original provenance and record the execution segments rather than relabeling M1 work as M4 work.

This supersedes the preceding hardware amendment's whole-seed allocation for seed 505. That seed now has a hardware confound and its elapsed times are not clean within-host comparisons. Retain it transparently in the registered n=10 analysis, report its mixed execution, and provide all outcomes without seed 505 as a descriptive sensitivity view, not a replacement primary result or additional significance claim. Seeds 500–504 remain wholly Linux; 506–509 wholly M4. Do not claim a universal compiler speedup from pooled seconds. Concurrent M4 compiler-confirmation and efficiency workers introduce CPU/memory contention; disclose the overlap when interpreting timing in both studies.

The final test remains locked until all sixty models complete. Include this amendment and migration/environment receipts in the unlock hashes. Copy and hash-verify the checkpoint and frozen inputs before resuming. The MacBook worker remains stopped. Mini-to-Linux checkpoint transfer is direct over Tailscale; the existing backup relay may still run on the MacBook until moved separately. No raw labels, final test scores, loss settings or stopping rules are changed by this migration.
