# Compiler efficiency: prospective hardware allocation amendment

2026-09-15. The user requested use of the now-idle MacBook after its topology cohort completed. At request time eight Linux models were complete, seed 501 relational was running, and all seed 505–509 models were unstarted. Interim validation had been inspected; the final test remained locked. This is a disclosed mid-study operational amendment, not an untouched single-machine preregistration.

Allocate full seed blocks 500–504 to Omarchy and 505–509 to MacBook M1. All six methods within a seed remain together and retain their original randomized order, frozen initialization seeds, four CPU threads, graph, teacher/cache, datasets, schedules, losses, optimizers, checkpoints and 256-update budget. No seeds are selected by outcome or discarded. Preserve the original config, preregistration and frozen input files byte-for-byte; this document supersedes only their single-machine and sequential-across-seeds statements. Record MacBook package versions separately; backend/OS floating-point differences remain a limitation.

Retain the registered ten within-seed contrasts and multiplicity correction. Report every seed and host and provide host-separated outcomes descriptively, without selecting a winning host or promoting subgroup results. Hardware is coupled to seed blocks, so do not infer a hardware effect. Compare wall-clock efficiency only within host; include archive transfer overhead and disclose shared teacher-cache preparation costs originated on Linux. Fixed updates/presentations remain matched. Final evaluation remains centralized on Linux after all 60 final and 128-prefix checkpoints are verified, with the amendment and execution wrapper included in the unlock hashes. No final test is evaluated by the MacBook worker.

Omarchy's active training child must finish unchanged. Replace only the scheduling parent; its replacement waits for that child before resuming the original controller. At MacBook-assigned jobs, the coordinator waits for immutable completion receipts rather than launching duplicate training. The MacBook sends each checkpoint to Linux using atomic writes and SHA-256 verification; the existing Linux-to-Seagate relay preserves all checkpoints. Only the replaceable MacBook model copy is removed after every checkpoint has been imported and the completion receipt published. Connectivity failure pauses transfer and retains local data. Preserve all negative results and the original all-60-model stopping gate.
